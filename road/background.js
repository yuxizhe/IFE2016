var Background = function (row,col,target,wallPic) {
	this.element = document.getquerySelector(target);
	this.row = row;
	this.col = col;
	this.wallPic = wallPic;
	this.Map = [];
	for(var i = 0,col = [];i<this.col;i++){
		this.Map[i]= col;
	};
	this.init();
}

Background.prototype.init = function() {
	
};

Background.prototype.randomPath = function() {
	var i = 0,j=0;
	while(i != this.row && j != this.col){
		if(Math.)
	}
};

Background.prototype.randomWall = function() {
	
};

Background.prototype.render = function() {
	
};